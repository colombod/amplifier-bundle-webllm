bundle:
  name: webllm
  version: 0.1.0
  description: |
    WebLLM bundle for browser-based local LLM inference via WebGPU.
    
    Default: CLI-safe context for understanding WebLLM/WebGPU patterns.
    For browser runtime: use behaviors/webllm-runtime explicitly.

includes:
  - bundle: webllm:behaviors/webllm-context
